upzip or copy the two subdirectories "hardware" and "libraries" into the "Documents/Arduino" subfolder.  

This has been used to work in Arduino IDE 1.6.3 and newer.  Older versions of the Arduino IDE will not work with these files (there has been a change in the way that the IDE reads in boards and hardware using the "boards.txt" file)

(last update:  2015 May 5, jnakane@physics.ubc.ca)